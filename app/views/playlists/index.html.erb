<div class="cards">
  <% @playlists.each do |playlist|%>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path playlist.photo.key, crop: :fill %>')">
      <%= playlist.name %>
    </div>
  <% end %>
</div>
